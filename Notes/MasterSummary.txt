Please read the following context carefully to continue our project seamlessly.

--- PROJECT STATE SUMMARY ---
We are building a React.js (Vite + React-Bootstrap + Tailwind CSS) Competitive Programming Platform / Online Judge website ("MOJ"). 
- The Frontend has been completely reviewed, debugged (fixed an initial node_modules/Vite dependency mismatch by using '--legacy-peer-deps'), and documented for my reference notes.
- The UI templates are verified and working, currently waiting for live database records.

--- WHAT WE COMPLETED (FRONTEND CODE MAP) ---
We have thoroughly analyzed and documented the following 16 files:
1. main.jsx / App.jsx: React Router DOM integration mapping routes like '/Problems/:id' and '/contests/:id'.
2. App.css / index.css: Tailwind utilities config and custom elements like '.difficulty-circle' filling dynamically based on CSS variables.
3. Blogs.jsx: Layout mapping out blog posts with responsive Bootstrap grid structures.
4. Contests.jsx / ContestDetail.jsx: Tracks contest lifecycles with a real-time 1-second countdown sync timer built via setInterval hooks.
5. Home.jsx / Difficulty.jsx: Pure layout dashboard blueprints displaying difficulty meters and using polymorphic 'as={Link}' buttons.
6. Header.jsx: Live sync user navigation layout monitoring browser 'localStorage' and global window EventListeners ('mojUserChanged').
7. LoginRegister.jsx: Dynamic auth layouts handling nested form submissions via object spread copying operators ('...').
8. Editor.jsx (IDE): Advanced engine rendering Monaco Editor ('vs-dark') with optional chaining operators parsing real-time judging results.
9. ProblemSet.jsx / ProblemDetail.jsx: Typesets LaTeX equations using <MathJaxContext>, matching complex nested tag maps and arrays.
10. Profile.jsx: Statistical user dashboard implementing sequential Promise chains (.then) and JS Map models to deduplicate contest attempts.
11. ExampleAPIUsage.jsx: Developer testing script playground illustrating base REST CRUD triggers.

--- BACKEND DIRECTORY TREE OVERVIEW ---
The backend uses an Express server with a Prisma ORM framework communicating with PostgreSQL.
- server/
  ├── middleware/ (auth.js)
  ├── prisma/ (schema.prisma, migrations/)
  ├── routes/ (auth, blogs, contests, execute, problems, sessionAuth, submissions, users)
  ├── utils/ (codeExecutor.js)
  ├── .env.example
  └── server.js

--- TARGET ACTIONS FOR THIS SESSION ---
We are now entering the backend server setup and database connection phase. Please help me drive this home in the following sequence:

1. CONNECT POSTGRESQL & PRISMA: Guide me through setting up my local pgAdmin PostgreSQL credentials inside a local .env configuration file, formatting the DATABASE_URL connection string correctly, and executing Prisma commands ('prisma db push' or 'prisma generate') to materialize our tables.
2. DISSECT SERVER.JS & SCHEMA.PRISMA: Walk me through the backend schema architectures to see how the User, Contest, Problem, and Submission models link up.
3. SEED INITIAL PROBLEMS DATA: Help me craft or run a migration script to populate our database with basic test items (like 'Two Sum', 'Fibonacci') so we can see them load live on the frontend.
4. SYSTEM END-TO-END RE-TEST: Help me fire up both the frontend and backend servers together to confirm data streams smoothly into our components.

Let me know you understand this context, and ask me for the contents of the database setup or the first backend files to get started!



A:

Please read this context carefully to act as my isolated Backend Engineering Assistant. 

--- ARCHITECTURAL OVERVIEW ---
We are developing a React.js (Vite) Competitive Programming Platform backed by an Express server using Prisma ORM (v7.9.1) connected to local PostgreSQL. End-to-end network handshakes are verified. Seeding parameters successfully inject sample challenge states into the tables. 

--- THE SYSTEM BACKEND TREE ---
- server/
  ├── middleware/ (auth.js)
  ├── prisma/ (schema.prisma, seed.js)
  ├── routes/ (auth.js, problems.js, contests.js, blogs.js, execute.js, submissions.js)
  └── utils/ (codeExecutor.js)

--- OUR BACKEND OBJECTIVES ---
1. REVIEW SECURITY MIDDLEWARE: Analyze 'middleware/auth.js' and 'routes/auth.js' to evaluate session authentication checks.
2. SANDBOX TESTING ('execute.js'): Trace how 'utils/codeExecutor.js' handles standard inputs/outputs, isolates user runtimes, and scores files against seeded test case JSON parameters.
3. ADMIN API HOOKS: Optimize creation API endpoints so an admin user can write new coding challenges straight into pgAdmin from a web view instead of using seed scripts.

Let me know you understand this backend engineering blueprint, and ask me to provide the code for 'server.js', 'schema.prisma', or our execution utilities to begin.


B:

Please read this context carefully to act as my isolated Frontend UI/UX Design Specialist.

--- ARCHITECTURAL OVERVIEW ---
We are optimizing the user interface for a React.js Competitive Programming Platform ("MOJ"). The platform is fully connected to a local Express/PostgreSQL database server. Data flows cleanly into our states, and countdown timers update live using custom 1-second interval hooks. We are using React-Bootstrap combined with utility layers from Tailwind CSS (@import "tailwindcss/utilities").

--- THE SYSTEM FRONTEND ROUTING TREE ---
- App.jsx Layout Center:
  ├── '/' -> Home.jsx (Landing platform dashboard)
  ├── '/Problemset' -> ProblemSet.jsx (Global index listing tables)
  ├── '/Problems/:id' -> ProblemDetail.jsx + Editor.jsx (Split-pane view + Monaco IDE)
  ├── '/contests' & '/contests/:id' -> Overviews and dynamic countdown tracking grids
  ├── '/blogs' -> Blogs.jsx Cards
  └── '/login' & '/profile' -> Session management screens

--- OUR FRONTEND OBJECTIVES ---
1. MATERIALIZE THE RANKINGS LAYOUT: Help me build a beautiful 'Ranking.jsx' component view from scratch. It must pull profiles from the user API and list competitors inside a responsive leaderboard table sorted descending by code rating integers.
2. POLISH HOME & GLOBAL LAYOUTS: Enhance 'Home.jsx' and 'Header.jsx' using custom Tailwind utilities, hover parameters, gradients, and polished typography to look like professional platform dashboards.
3. OPTIMIZE SPLIT-PANE DESIGNS: Refine 'ProblemDetail.jsx' and 'Editor.jsx' to ensure descriptions and the Monaco Code engine scale cleanly on smaller monitors.

Let me know you understand this UI/UX optimization blueprint, and ask me to provide our global 'App.jsx' layout or any style files to start designing!
