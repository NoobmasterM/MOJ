VITE_API_URL=http://localhost:3000/api
# Server-only values (place these in server/.env; never commit that file)
# DATABASE_URL=postgresql://USER:PASSWORD@localhost:5432/moj
# JWT_SECRET=replace-with-a-long-random-secret
